import styled from "styled-components";
export const DropdownStyles = styled.div`
  button {
    /* DropdownMenu.Item normally has full width, but I guess with asChild I need to set it myself */
    width: 100%;
  }
`;
